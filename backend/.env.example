POSTGRES_DB=gateway
POSTGRES_USER=gateway
POSTGRES_PASSWORD=gateway
CORS_ALLOWED_ORIGIN=http://localhost:5173
APP_BASE_URL=http://localhost:8080
MCP_OAUTH_USER=user
MCP_OAUTH_PASSWORD=password
ADMIN_OAUTH_USER=user
ADMIN_OAUTH_PASSWORD=password
OAUTH_CLIENT_ID=aifindr
OAUTH_CLIENT_SECRET=aifindr-dev-secret

# PKCS#8 PEM (single line with \n). Generate:
#   openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out private.pem
# Then paste contents with newlines as \n, or set multiline secret in Render.
OAUTH_JWK_KEY_ID=gateway-1
OAUTH_RSA_PRIVATE_KEY=

